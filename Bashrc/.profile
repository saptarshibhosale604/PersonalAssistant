# echo "This is the profile START"
# refreshApp() { rm /App/* -rf && cp /root/ProjectRpi/Rpi/PersonalAssistant/* /App/ -r; }
# echoTxt() { echo 'Hi there'; }
alias refreshApp="rm /App/* -rf && cp /root/ProjectRpi/Rpi/PersonalAssistant/* /App/ -r"
alias echoTxt="echo 'Hi there'"
# echo "echoTxt refreshApp"
